<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="main bg-zinc-900 w-full min-h-screen text-white">
      <div class="w-full p-4">
        <form action="/create" method="post" class="flex flex-col mt-2 items-start">
          <input
            type="text"
            class="bg-zinc-700 rounded-md p-2 m-2 w-full outline-none"
            placeholder="Enter task name"
            name="title"
            required="true"
          />
          <textarea
            class="bg-zinc-700 rounded-md p-2 m-2 w-full resize-none outline-none"
            placeholder="Enter the description"
            name="details"
            required="true"

          ></textarea>
          <button
            class="bg-blue-700 rounded-md py-2 px-4 mt-2 ml-2 cursor-pointer"
          >
            click me
          </button>
        </form>
      </div>
      <div class="task-container text-white p-4 mt-4 flex gap-4 flex-wrap">
        <% if(files.length > 0 ){%>
            <% files.forEach(function(ele){%>

              <div class="task p-2 bg-zinc-700 h-20 w-60 rounded-md tracking-tighter">
                <div class="p-1"><%=ele%></div>
                <a class="more text-blue-500 p-1 cursor-pointer" href="/files/<%=ele%>">read more...</a>
              </div>
              <% })%>
          <% } else {%>
            <div class="text-zinc-700 text-xl">No tasks yet..</div>
            <% }%>
      </div>
    </div>
  </body>
</html>
